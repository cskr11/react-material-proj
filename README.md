### Project for material ui learning

Download the code & run **npm install** to install dependencies before starting the app.

Add Material UI as a dependency by running **npm install @material-ui/core**
